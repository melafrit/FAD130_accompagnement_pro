# === Documents de référence du cours — NON versionnés ===
# Droits d'auteur (Cnam, Maela Paul, etc.) : supports de cours,
# transcriptions et fiche module ne sont pas publiés sur le dépôt.
Doc_references/

# === Fichiers système / éditeur ===
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*~
.vscode/
.idea/

# === Fichiers temporaires Office (verrous ~$) ===
~$*

# === Application Boussole : secrets, dépendances, build, données ===
# Secrets et variables d'environnement (JAMAIS sur GitHub)
.env
*.env
!*.env.example
# Dépendances et build
node_modules/
dist/
build/
# Base de données (contient des données personnelles — JAMAIS sur GitHub)
*.sqlite
*.sqlite3
*.db
# Données runtime, sauvegardes et certificats TLS
app/data/
backups/
acme.json
letsencrypt/
