{
  "name": "boussole-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Batterie de tests de non-régression (ISTQB) — Boussole",
  "type": "module",
  "scripts": {
    "test:unit": "vitest run unit",
    "test:api": "vitest run api",
    "test:ui": "playwright test",
    "report": "node scripts/report.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.48.0",
    "vitest": "^2.1.0"
  }
}
